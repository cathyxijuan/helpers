% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_regression.R
\name{simulate_regression}
\alias{simulate_regression}
\title{Simulate a dataset and fit a linear regression model}
\usage{
simulate_regression(n = 100, beta0 = 2, beta1 = 0.5, sigma = 1,
  seed = NULL)
}
\arguments{
  \item{n}{Integer number of observations to simulate. Defaults to 100.}

  \item{beta0}{Numeric intercept used in the data-generating process. Defaults to 2.}

  \item{beta1}{Numeric slope used in the data-generating process. Defaults to 0.5.}

  \item{sigma}{Standard deviation of the Gaussian noise added to the outcome. Defaults to 1.}

  \item{seed}{Optional integer seed to ensure reproducibility. If \code{NULL}, the current random seed is used.}
}
\value{
A list containing two elements: \code{data}, a data frame with the simulated predictor \code{x} and outcome \code{y}, and \code{model}, the \code{lm} object produced by fitting \code{y ~ x} to the simulated data.
}
\description{
This helper simulates a simple linear regression dataset and fits a model using \code{lm}. The function returns both the simulated data and the fitted model so that you can inspect the coefficients, fitted values, or residuals.
}
\examples{
result <- simulate_regression(n = 50, beta0 = 1, beta1 = 0.75, sigma = 1)
result$model
}
